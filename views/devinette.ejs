<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Jeu de Devinette</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="style.css">
</head>
<body>
   
    <h1 class="main-title">EXPRESS BRAINS</h1>
    <p class="riddle-description">Découvrez à un minimum de tour, le nombre qui se cache derrière la carte mistère !</p>
    <!-- Form -->
    <form action="/devinette" method="post" class="riddle-form">
        <div class="card text-center" style="width: 20rem; margin: auto;">

            <!-- image of decouvert number -->
            <% if (isWin) { %>
            <div class="display-1 p-4"><%= guessedNumber %></div>
            <% } else { %>
                <img src="/question-mark.jpg" class="card-img-top" alt="question mark image">
            <% } %>

                <div class="card-body">
                    <% if (!isWin) { %>
                        <label for="number" class="card-title center">
                            <strong>Qui suis-je ?</strong><br>
                            Quel nombre se cache derrière cette carte mystère ?
                        </label>
                    <% } else { %>
                        <p class="p-success">Bravo !</p>
                    <% } %>
                

                <!-- hidden if win -->
                <% if (!isWin) { %>
                    <input type="number" id="number" name="number" class="form-control mt-3" placeholder="?" required>
                <% } %>
            </div>

            <div class="card-body">
                <% if (isWin) { %>
                    <a href="/" class="btn btn-success">Rejouer</a>
                <% } else { %>
                    <button type="submit" class="btn btn-primary">J'ai trouvé</button>
                <% } %>
            </div>
        </div>
    </form>
    <!-- Display message -->
    <% if (message) { %>
        <p class="alert <%= isWin ? 'alert-success' : 'alert-danger' %>" role="alert">
            <strong><%= message %></strong>
        </p>
    <% } %>

   

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
</body>
</html>